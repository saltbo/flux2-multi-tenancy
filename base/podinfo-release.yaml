apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: apps
spec:
  serviceAccountName: dev-team
  releaseName: podinfo
  chart:
    spec:
      chart: podinfo
      version: '5.2.1'
      sourceRef:
        kind: HelmRepository
        name: podinfo
  interval: 5m
